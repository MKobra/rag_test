# Журнал задачи

## Цель

Создать правила для senior full-stack разработчика: не выполнять операции с Git без явного разрешения пользователя, писать подробные сообщения коммитов и фиксировать логику решения в Markdown.

## Критерий успеха

В корне проекта есть `AGENTS.md` с обязательным алгоритмом работы, правилами журналирования и запретом на `commit`, `push` и создание PR без разрешения пользователя.

## Исходные данные

- Проект пустой.
- Существующих `AGENTS.md` и Markdown-файлов не было.

## Выполненные действия

1. Проверена структура корня проекта.
2. Создан `AGENTS.md` с правилами работы, алгоритмом действий, требованиями к `TASK_LOG.md` и Git-ограничениями.
3. Создан этот журнал для текущей задачи.

## Проверки

- Проверено наличие файлов после создания.

## Ограничения и риски

- Автоматические тесты, линтер и typecheck в пустом проекте отсутствуют.

## Реализация RAG-сервиса

### Цель

Создать LangChain-сервис с FastAPI, PostgreSQL/pgvector, локальными embeddings и Groq, с отдельной вкладкой для каждого документа.

### Выполненные действия

1. Инициализирован Git и создана ветка `feature/rag-pgvector` от `main`.
2. Добавлена базовая структура проекта, `.gitignore`, `.env.example`, Docker Compose и документация архитектуры.

### Ограничения

- Реализация выполняется поэтапно.
- Секреты не записываются в журнал и не добавляются в Git.

### Последовательность коммитов

- `97c8ad8` — начальная структура проекта и документация.
- `05308e6` — backend индексации документов и поиска.
- `14314bc` — HTML/CSS/JavaScript интерфейс.

### Следующий этап

Добавлены тесты загрузчиков, health endpoint и безопасного ответа без релевантного контекста. Перед финальным коммитом требуется выполнить тесты в окружении с установленными зависимостями.

### Проверки окружения

- `.venv` создан локально и зависимости из `requirements.txt` установлены.
- `pytest`: 5 тестов прошли.
- `python -m compileall -q app tests`: прошёл.
- `docker compose config`: прошёл.
- PostgreSQL/pgvector запущен через Docker Compose.
- Расширение `vector` и таблица `documents` успешно созданы.
- `GET /api/health`: HTTP 200.
- HTTP upload через FastAPI TestClient: успешно, ответ `201`, `chunk_count=1`.

### Исправления после runtime-проверки

- `ec63661` — добавлены `uploaded_at`, корректный PGVector-фильтр и строгая очистка источников при отсутствии ответа.

### Итоговое состояние перед слиянием

- Тесты: 5 passed.
- Синтаксис Python: пройден.
- Конфигурация Docker Compose: пройдена.
- PostgreSQL/pgvector: работает локально.
- Индексация и similarity search: проверены на тестовом документе.
- HTTP upload: проверен.
- Секрет `.env` не отслеживается Git.

## Безопасность, аккаунты и чаты

Добавлены простая регистрация/вход, PBKDF2-хэши паролей, подписанные bearer-токены, owner-scoped документы, отдельные чаты и история сообщений. Лимиты MVP: 10 МБ файл, 30 документов, 10 загрузок/час, 20 вопросов/минуту, 2000 символов вопроса и 8 сообщений истории в prompt.

Добавлена генерация короткой темы документа через Groq с fallback на имя файла. Исходное имя файла сохраняется и показывается под темой.

Интерфейс переработан в визуальный стиль цифрового архива: авторизация, документы, чаты, сообщения ассистента, источники и responsive layout.
